<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경색 설정 -->
    <solid android:color="@color/sundo_green_dark" />

    <!-- 버튼의 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/sundo_green_dark"/>

    <!-- 둥근 모서리 설정 -->
    <corners android:radius="16dp" />
</shape>