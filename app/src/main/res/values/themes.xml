<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sundo_project_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- 240813 이승연 추가-->
        <item name="android:statusBarColor">@android:color/white</item> <!-- 상단바 배경색을 흰색으로 설정 -->
        <item name="android:windowLightStatusBar">true</item> <!-- 상태바 아이콘을 어두운 색으로 설정 -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Sundo_project_app" parent="Base.Theme.Sundo_project_app" />
</resources>